# clasp — contains the private scriptId, never commit
.clasp.json
.clasprc.json

# Local secrets / notes
*.local
.env
secrets/

# Real business data must not be committed
data/
export/
*.csv
*.pdf

# Office temp/lock files (created while a file is open)
~$*

# OS / editor
.DS_Store
Thumbs.db
.vscode/settings.json
node_modules/
secret
