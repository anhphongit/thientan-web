/**
 * Main.gs — web app entry point.
 *
 * STATUS: stub. No implementation yet.
 *
 * Responsibility:
 *   - doGet(e): serve the single-page UI via HtmlService, with
 *     XFrameOptionsMode.ALLOWALL and a viewport meta for mobile
 *   - include(filename): partial-include helper for ui/*.html
 *   - A single place to wrap handlers so thrown errors return a clean
 *     Vietnamese message instead of a raw stack trace
 *
 * Rules:
 *   - No business logic here. Routing and shell only.
 */
